# Render Blueprint Specification for JoinEz (Full Stack)
services:
  # 1. PostgreSQL Managed Database
  - type: postgres
    name: joineazy-postgres
    plan: free
    ipAllowList: [] # only allow internal services

  # 2. Express Backend Web Service
  - type: web
    name: joineazy-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        fromDatabase:
          name: joineazy-postgres
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CLIENT_URL
        value: "*" # update to frontend domain once deployed
    healthCheckPath: /api/health

  # 3. Vite React Frontend Static Site
  - type: static
    name: joineazy-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: joineazy-backend
          envVarKey: RENDER_EXTERNAL_URL
          transform: "${RENDER_EXTERNAL_URL}/api"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
